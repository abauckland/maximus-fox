   <div class="current">      <div id="toolbar">        <%= link_to "Back", project_sections_project_path(@current_project.id), :class => "back", :rel => "external" %>      <h1><%= @current_project.code %> rev <%= @current_revision_rev %></h1>      </div>        <% unless flash.empty? %>        <div class="info">        <%- flash.each do |name, msg| -%>          <%= content_tag :div, msg, :id => "flash_#{name}" %>        <%- end -%>        </div>      <% end %>      <ul>  <%@project_subsections.each do |subsection| %>  <li class="arrow"><%= link_to "#{subsection.section.ref}#{subsection.ref} #{subsection.text}", {:controller => "projects", :action => "show", :id => @current_project.id, :subsection => subsection.id}%></li>      <% end %>  </ul>      </div>      